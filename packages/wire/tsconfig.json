{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // For the tests only: they serve the .wasm over loopback so the scanner never reaches the network.
    // `src` is browser code and must not import a Node builtin.
    "types": ["node"]
  },
  "include": ["src", "test"]
}
