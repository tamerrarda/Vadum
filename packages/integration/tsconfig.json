{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // The loopback test serves the scanner's .wasm over loopback, so the QR hop is real.
    "types": ["node"]
  },
  "include": ["test"]
}
